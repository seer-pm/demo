/** DOM id of an outcome's pool-details panel, shared by the toggle (aria-controls) and the panel. */
export const getOutcomeActivePanelId = (outcomeIndex: number) => `outcome-pool-details-${outcomeIndex}`;
